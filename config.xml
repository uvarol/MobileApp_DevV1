<?xml version="1.0" encoding="UTF-8"?>
<widget xmlns="http://www.w3.org/ns/widgets" xmlns:gap="http://phonegap.com/ns/1.0" id="com.kiraca.employeedirectory" versionCode="1" version="1.0.0">

    <!-- versionCode is optional and Android only -->

    <name>Employee Directory</name>

    <description>
        A simple employee directory application
    </description>

    <author href="Genel Profilhttps://tr.linkedin.com/in/umitvarol" email="uvarol@gmail.com">
        Ümit Varol
    </author>

    <!--
        If you do not want any permissions to be added to your app, add the
        following tag to your config.xml; you will still have the INTERNET
        permission on your app, which PhoneGap requires.
    -->
    <preference name="permissions" value="none"></preference>

    <!-- Customize your app and platform with the preference element. -->
    <preference name="orientation" value="default"></preference>        <!-- all: default means both landscape and portrait are enabled -->
    <preference name="target-device" value="universal"></preference>      <!-- all: possible values handset, tablet, or universal -->
    <preference name="fullscreen" value="true"></preference>           <!-- all: hides the status bar at the top of the screen -->
    <preference name="webviewbounce" value="true"></preference>           <!-- ios: control whether the screen 'bounces' when scrolled beyond the top -->
    <preference name="prerendered-icon" value="true"></preference>           <!-- ios: if icon is prerendered, iOS will not apply it's gloss to the app's icon on the user's home screen -->
    <preference name="stay-in-webview" value="false"></preference>          <!-- ios: external links should open in the default browser, 'true' would use the webview the app lives in -->
    <preference name="ios-statusbarstyle" value="black-opaque"></preference>   <!-- ios: black-translucent will appear black because the PhoneGap webview doesn't go beneath the status bar -->
    <preference name="detect-data-types" value="true"></preference>           <!-- ios: controls whether data types (such as phone no. and dates) are automatically turned into links by the system -->
    <preference name="exit-on-suspend" value="false"></preference>          <!-- ios: if set to true, app will terminate when home button is pressed -->
    <preference name="show-splash-screen-spinner" value="true"></preference>           <!-- ios: if set to false, the spinner won't appear on the splash screen during app loading -->
    <preference name="auto-hide-splash-screen" value="true"></preference>           <!-- ios: if set to false, the splash screen must be hidden using a JavaScript API -->
    <preference name="disable-cursor" value="false"></preference>          <!-- blackberry: prevents a mouse-icon/cursor from being displayed on the app -->
    <preference name="android-installLocation" value="auto"></preference>           <!-- android: app install location. 'auto' will choose. 'internalOnly' is device memory. 'preferExternal' is SDCard. -->
    <!--
        Define a specific version of PhoneGap to build into your app.
        <preference name="phonegap-version"       value="3.5.0" />
    -->

    <!-- Plugins -->

    <!-- Core plugins -->
    <plugin name="cordova-plugin-battery-status"></plugin>
    <plugin name="cordova-plugin-camera"></plugin>
    <plugin name="cordova-plugin-media-capture"></plugin>
    <plugin name="cordova-plugin-console"></plugin>
    <plugin name="cordova-plugin-contacts"></plugin>
    <plugin name="cordova-plugin-device"></plugin>
    <plugin name="cordova-plugin-device-motion"></plugin>
    <plugin name="cordova-plugin-device-orientation"></plugin>
    <plugin name="cordova-plugin-dialogs"></plugin>
    <plugin name="cordova-plugin-file"></plugin>
    <plugin name="cordova-plugin-file-transfer"></plugin>
    <plugin name="cordova-plugin-geolocation"></plugin>
    <plugin name="cordova-plugin-globalization"></plugin>
    <plugin name="cordova-plugin-inappbrowser"></plugin>
    <plugin name="cordova-plugin-media"></plugin>
    <plugin name="cordova-plugin-network-information"></plugin>
    <plugin name="cordova-plugin-splashscreen"></plugin>
    <plugin name="cordova-plugin-vibration"></plugin>

    <!-- Third party plugins -->
    <!-- A list of available plugins are available at https://build.phonegap.com/plugins -->
    <!--
        <plugin name="com.phonegap.plugins.barcodescanner" />
    -->

    <!-- Define app icon for each platform. -->
    <icon src="icon.png"></icon>
    <icon src="res/icon/android/ldpi-v1/icon.png" gap:platform="android" gap:qualifier="ldpi"></icon>
    <icon src="res/icon/android/mdpi-v1/icon.png" gap:platform="android" gap:qualifier="mdpi"></icon>
    <icon src="res/icon/android/hdpi-v1/icon.png" gap:platform="android" gap:qualifier="hdpi"></icon>
    <icon src="res/icon/android/xhdpi/icon.png" gap:platform="android" gap:qualifier="xhdpi"></icon>
    <icon src="res/icon/blackberry/icon-80.png" gap:platform="blackberry"></icon>
    <icon src="res/icon/blackberry/icon-80.png" gap:platform="blackberry" gap:state="hover"></icon>
    <icon src="res/icon/ios/icon-57.png" gap:platform="ios" width="57" height="57"></icon>
    <icon src="res/icon/ios/icon-72.png" gap:platform="ios" width="72" height="72"></icon>
    <icon src="res/icon/ios/icon-57-2x.png" gap:platform="ios" width="114" height="114"></icon>
    <icon src="res/icon/ios/icon-72-2x.png" gap:platform="ios" width="144" height="144"></icon>
    <icon src="res/icon/webos/icon-64.png" gap:platform="webos"></icon>
    <icon src="res/icon/windows-phone/icon-48.png" gap:platform="winphone"></icon>
    <icon src="res/icon/windows-phone/icon-173.png" gap:platform="winphone" gap:role="background"></icon>

    <!-- Define app splash screen for each platform. -->
    <gap:splash src="res/screen/android/screen-ldpi-portrait.png" gap:platform="android" gap:qualifier="port-ldpi"></gap:splash>
    <gap:splash src="res/screen/android/screen-mdpi-portrait.png" gap:platform="android" gap:qualifier="port-mdpi"></gap:splash>
    <gap:splash src="res/screen/android/screen-hdpi-portrait.png" gap:platform="android" gap:qualifier="port-hdpi"></gap:splash>
    <gap:splash src="res/screen/android/screen-xhdpi-portrait.png" gap:platform="android" gap:qualifier="port-xhdpi"></gap:splash>
    <gap:splash src="res/screen/blackberry/screen-225.png" gap:platform="blackberry"></gap:splash>
    <gap:splash src="res/screen/ios/screen-iphone-portrait.png" gap:platform="ios" width="320" height="480"></gap:splash>
    <gap:splash src="res/screen/ios/screen-iphone-portrait-2x.png" gap:platform="ios" width="640" height="960"></gap:splash>
    <gap:splash src="res/screen/ios/screen-iphone-portrait-568h-2x.png" gap:platform="ios" width="640" height="1136"></gap:splash>
    <gap:splash src="res/screen/ios/screen-ipad-portrait.png" gap:platform="ios" width="768" height="1024"></gap:splash>
    <gap:splash src="res/screen/ios/screen-ipad-landscape.png" gap:platform="ios" width="1024" height="768"></gap:splash>
    <gap:splash src="res/screen/windows-phone/screen-portrait.jpg" gap:platform="winphone"></gap:splash>

    <gap:config-file platform="ios" parent="CFBundleShortVersionString">
    <string>100</string>
    </gap:config-file>

    <!--
        Define access to external domains.
        <access />            - a blank access tag denies access to all external resources.
        <access origin="*" /> - a wildcard access tag allows access to all external resource.
        Otherwise, you can specify specific domains:
        <access origin="http://phonegap.com" />                    - allow any secure requests to http://phonegap.com/
        <access origin="http://phonegap.com" subdomains="true" />  - same as above, but including subdomains, such as http://build.phonegap.com/
        <access origin="http://phonegap.com" browserOnly="true" /> - only allows http://phonegap.com to be opened by the child browser.
    -->

    <access origin="*"></access>
    <!-- Added the following intents to support the removal of whitelist code from base cordova to a plugin -->
    <!-- Whitelist configuration. Refer to https://cordova.apache.org/docs/en/edge/guide_appdev_whitelist_index.md.html -->

    <access origin="https://apex.kiraca.com.tr" subdomains="true"></access>

    <plugin name="cordova-plugin-whitelist" version="1"></plugin>
    <allow-intent href="http://*/*"></allow-intent>
    <allow-intent href="https://*/*"></allow-intent>
    <allow-intent href="tel:*"></allow-intent>
    <allow-intent href="sms:*"></allow-intent>
    <allow-intent href="mailto:*"></allow-intent>
    <allow-intent href="geo:*"></allow-intent>
    <platform name="android">
        <allow-intent href="market:*"></allow-intent>
    </platform>
    <platform name="ios">
        <allow-intent href="itms:*"></allow-intent>
        <allow-intent href="itms-apps:*"></allow-intent>
    </platform>

	
</widget>
